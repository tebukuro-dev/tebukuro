{
  "name": "web-component",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/*.* --bundle --minify --outfile=dist/tebukuro-form.js",
    "serve": "npm-run-all -p serve:*",
    "serve:watch": "yarn build --watch",
    "serve:start": "yarn build --servedir=dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-to-webcomponent": "^2.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.15.16",
    "npm-run-all": "^4.1.5"
  }
}
